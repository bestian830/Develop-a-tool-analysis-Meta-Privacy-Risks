# 隐私政策分析器 - 依赖包

# 核心NLP库
spacy>=3.7.0
# 需要下载模型: python -m spacy download en_core_web_sm

# Transformer模型（用于SRL参数提取）
transformers>=4.35.0
torch>=2.0.0

# 注意：
# - AllenNLP安装较复杂且不兼容Python 3.13，改用Transformers
# - 使用Transformer NER模型进行SRL参数提取（+125%效果提升）

# 数据处理
numpy>=1.24.0
pandas>=2.0.0

# LLM API（可选）
requests>=2.28.0  # 用于调用 LLM API

# 报告生成
markdown>=3.4.0

# 可选：用于可视化
# matplotlib>=3.7.0
# seaborn>=0.12.0

# 开发工具
pytest>=7.4.0  # 用于测试
black>=23.0.0  # 代码格式化

# Web框架和API
flask>=2.3.0
flask-cors>=4.0.0
sqlalchemy>=2.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
chardet>=5.0.0









